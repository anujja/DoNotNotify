source "https://rubygems.org"

# Play Store publishing (fastlane supply). Run `bundle lock` once locally to
# pin exact versions; CI resolves within these constraints until then.
gem "fastlane", "~> 2.22"
